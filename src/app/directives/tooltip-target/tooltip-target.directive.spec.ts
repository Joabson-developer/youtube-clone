import { TooltipTargetDirective } from './tooltip-target.directive';

describe('TooltipTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTargetDirective();
    expect(directive).toBeTruthy();
  });
});
